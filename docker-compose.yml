version: '3.8'

services:
  # Redis service for Celery broker and job queue
  redis:
    image: redis:7-alpine
    container_name: MedScribe-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  # FastAPI application
  api:
    build: .
    container_name: MedScribe-api
    ports:
      - "8000:8000"
    environment:
      - MedScribe_REDIS_HOST=redis
      - MedScribe_REDIS_PORT=6379
      - MedScribe_CELERY_BROKER_URL=redis://redis:6379/0
      - MedScribe_CELERY_RESULT_BACKEND=redis://redis:6379/0
    env_file:
      - .env
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ./output:/app/output
      - ./temp:/tmp/MedScribe
      - .:/app  # Mount code for development
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
    restart: unless-stopped

  # Celery worker for background tasks
  celery:
    build: .
    container_name: MedScribe-celery
    environment:
      - MedScribe_REDIS_HOST=redis
      - MedScribe_REDIS_PORT=6379
      - MedScribe_CELERY_BROKER_URL=redis://redis:6379/0
      - MedScribe_CELERY_RESULT_BACKEND=redis://redis:6379/0
    env_file:
      - .env
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ./output:/app/output
      - ./temp:/tmp/MedScribe
      - .:/app  # Mount code for development
    command: celery -A tasks worker --loglevel=info --pool=solo
    restart: unless-stopped

volumes:
  redis_data:
    driver: local
